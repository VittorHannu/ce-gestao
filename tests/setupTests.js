/*
 * Este arquivo de configuração (`setupTests.js`) é usado para preparar o ambiente de testes
 * antes que eles sejam executados. Ele importa a biblioteca `@testing-library/jest-dom`,
 * que adiciona "matchers" (verificadores) personalizados para o Jest, facilitando
 * a escrita de testes que interagem com o DOM (a estrutura da página).
 *
 * Visualmente no seu site, este arquivo não tem nenhum impacto, pois ele só é
 * utilizado durante a execução dos testes automatizados para garantir que o
 * código funcione como esperado.
 *
 *
 *
 *
 */

import '@testing-library/jest-dom';